{
  "name": "pm2-cluster-cache",
  "version": "1.0.6",
  "description": "pm2 cluster memory cache with ttl and more possibilities of data store in cluster",
  "main": "lib/ClusterCache.js",
  "repository": {
    "type": "git",
    "url": "git@github.com:pavlikm/pm2-cluster-memory-cache.git"
  },
  "bugs": {
    "url": "https://github.com/pavlikm/pm2-cluster-memory-cache/issues"
  },
  "keywords": [
    "pm2",
    "cache",
    "cluster",
    "ttl"
  ],
  "scripts": {
    "build": "babel ./src --out-dir ./lib",
    "prepublish": "npm run build",
    "test": "cd test && npm install && npm run test",
    "test-self": "cd test && npm install && npm run self",
    "test-cluster": "cd test && npm install && npm run cluster",
    "test-master": "cd test && npm install && npm run master",
    "test-all": "cd test && npm install && npm run all"
  },
  "author": "pavlikm",
  "license": "MIT",
  "dependencies": {
    "pm2": "^4.5.5",
    "@pm2/io": "^4.3.5"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.24.1"
  }
}
